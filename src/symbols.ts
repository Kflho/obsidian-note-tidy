/**
 * 逐符号的空格规则（纯数据 + 纯函数，不依赖 Obsidian API）。
 *
 * ## 一条总原则：空格只用来分隔**不同语言的内容**
 *
 * 中文排版规范（W3C [中文排版需求 clreq](https://www.w3.org/TR/clreq/)）里，"加空隙"只有一种情况：
 * 汉字与**西文字母、阿拉伯数字**之间用不多于四分之一个汉字宽的字距或空白（§6.3.3）；
 * 同一个 §6.3.3 还写着「在中文点号前后、中文开始夹注符号之后、结束夹注符号之前的
 * 西文字母或阿拉伯数字，不调整字距或加入空白」，§1.2 更直接：汉字与标点都是 1:1 方块，
 * "**无缝隙并列**"。CSS 的 `text-autospace` 默认值（`ideograph-alpha` + `ideograph-numeric`）
 * 同样只在中西文之间加空隙，标点要另外显式写 `punctuation` 才加。
 *
 * 所以本表的 `space` 一律读作「**与西文内容（字母 / 数字 / 公式 / 行内代码）之间留一格**」：
 * 邻居是中文、全角标点、另一个符号时都按贴紧处理（已有的空格一并收掉）。
 *
 * - `甲&乙` → `甲&乙`（`&` 是 Unicode 标点 Po，中文与标点无缝隙）、`甲 & 乙` → `甲&乙`
 * - `A & B`、`$A$ & $B$` → 保留那一格（西文内容之间）
 * - `, ：`、`| ：`、`：&`、`7. 并列：&` → 全部贴紧（符号之间也不加）
 *
 * ## 逐符号规则
 *
 * - 英文符号 1：`,` `.` `!` `?` `:` 后面有空格（`word, word`）；
 * - 英文符号 2：`"` 左引号后、右引号前有空格 —— **右引号后面没有空格**；外侧按"分隔语言"办；
 * - 英文符号 3：`()` 前后没有空格、`...` 前后没有空格；
 * - latex 符号格式 4：`→` 与前后内容间要加空格；`|` 单独一个左右要加空格 —— 这两条写在
 *   笔记的「**代码格式** → latex 符号格式」一节，讲的是公式代码里的写法（公式里由
 *   「公式排版」latex-layout.ts 负责）；正文里按上面那条总原则走；
 * - latex 符号格式 5：`&` 前后有内容都要加排版空格（同上：正文里只与西文内容留一格）；
 * - latex 符号格式 2：`^` 与前后内容间不加空格；包裹符号（`()`、`||`）前后不加空格；
 * - 标记命名 4、5：`：` 是"内容后标注或解释"（紧跟内容）；`/`（或、别名）贴紧，没有空格规则。
 *
 * ## 包裹符号：圈住的东西是一个整体
 *
 * 括号 `（）` / `()`、书名号 `《》` `〈〉`、引号 `“”` `‘’`、成对的 `"` 是**表示"里面的内容"**
 * 的符号 —— 它们负责"圈"，**本身不算内容**：
 *
 * 1. **内侧不留空格**：`（ 内容 ）` → `（内容）`、`《 书名 》` → `《书名》`、`“ 引文 ”` → `“引文”`；
 * 2. **里面的符号不朝包裹符号要空格**：`（+）`、`（→）`、`（|）`、`"+"` 都贴紧；
 * 3. **圈住的那段文字内部照旧**：`《新 吊带袜天使》` 里专有名词的那一格保留（文字格式 4）；
 * 4. **外侧照"分隔语言"办**：西文内容旁留一格（`He said "hello" loudly`），中文旁贴紧
 *    （`他说 "你好" 了` → `他说"你好"了`，即英文符号 2 的"左引号前、右引号后没有空格"）。
 *
 * ## 竖线：先分清"数学记号里的"还是"正文里的"
 *
 * 全库实测（`|` 出现在正文而非表格 / 公式 / 双链里的地方）只有四种：
 * - `如（）、||等` —— 包裹符号，`||` 贴紧，两边都不加空格；
 * - `$0.00$ | $0.30$: $11.77$` —— 正文里的分隔符，西文内容之间各一格；
 * - `|：单独一个 | 左右要加空格` —— 被提到的符号，与 `：`、与中文都贴紧；
 * - `x̂_{k|k}`、`P(A|B)`、`|f_{y}|` —— 数学记号里的一部分，**贴紧、一个字符都不动**。
 *
 * 所以「单独一个 `|`」的判据是**贴不贴数学记号**：紧贴字母 / 数字 / `_` `^` `{` `}` `\`
 * 的竖线属于数学记号（规则 180 说的"单独一个"不包括它），其余才算正文里的独立符号。
 * `||`（连续两个及以上）整体是包裹符号，按 176 贴紧。
 */

/** 一个符号对自己左侧 / 右侧的要求 */
export type SymbolPad =
	/** 留一个空格 */
	| 'space'
	/** 不留空格 */
	| 'none'
	/** 不动，交给别的规则 */
	| 'keep';

export interface SymbolRule {
	left: SymbolPad;
	right: SymbolPad;
	/** 笔记里的出处，设置面板与测试对着它看 */
	source: string;
	/**
	 * `space` 的生效范围（默认 `content`）。
	 *
	 * - `content`：与**任何内容**（中文 / 西文 / 数字 / 公式 / 行内代码）之间留一格 ——
	 *   英文标点 `, . ! ? :` 用这一档（"英文符号遵循英文规则，在中文间可以有空格"：
	 *   `word, word`、`here. 中文`、`1.矩阵` → `1. 矩阵`）；
	 * - `latin`：只与**西文内容**（字母 / 数字 / 公式 / 行内代码）之间留一格，
	 *   中文旁、符号旁一律贴紧（已有的空格收掉）—— `&` `|` `→` 与引号外侧用这一档
	 *   （`A & B` 留一格，`甲 & 乙` → `甲&乙`、`他说 "你好" 了` → `他说"你好"了`）。
	 */
	scope?: 'content' | 'latin';
}

/** 单独一个 `|`（latex 符号格式 4）：与西文内容之间留一格，中文旁、符号旁贴紧 */
export const STANDALONE_PIPE_RULE: SymbolRule = {
	left: 'space',
	right: 'space',
	source: 'latex 符号格式 4（单独一个）',
	scope: 'latin',
};

/** 符号表：符号 → 自己的左右空格规则（键是一个 piece 的完整文本） */
export const SYMBOL_RULES: Record<string, SymbolRule> = {
	// 英文符号 1：标点前不留空格、后面空一格（与内容之间；符号旁贴紧）
	',': { left: 'none', right: 'space', source: '英文符号 1' },
	'.': { left: 'none', right: 'space', source: '英文符号 1' },
	'!': { left: 'none', right: 'space', source: '英文符号 1' },
	'?': { left: 'none', right: 'space', source: '英文符号 1' },
	':': { left: 'none', right: 'space', source: '英文符号 1' },
	// 标记命名 4：内容后标注或解释（紧跟内容，两侧贴紧）
	'：': { left: 'none', right: 'none', source: '标记命名 4' },
	// latex 符号格式 4：运算符号、关系符号和前后内容间要加空格；`|` 单独一个左右要加空格
	// —— 正文里只与西文内容留一格（中文旁贴紧，标点与中文无缝隙）
	'→': { left: 'space', right: 'space', source: 'latex 符号格式 4', scope: 'latin' },
	'|': STANDALONE_PIPE_RULE,
	// latex 符号格式 5：其他符号前后有内容都加排版空格（同上）
	'&': { left: 'space', right: 'space', source: 'latex 符号格式 5', scope: 'latin' },
	// latex 符号格式 2：修饰符号前后不加空格
	'^': { left: 'none', right: 'none', source: 'latex 符号格式 2' },
	// 英文符号 3：... 的现代写法中前后没有空格
	'...': { left: 'none', right: 'none', source: '英文符号 3' },
};

/**
 * 半角引号（成对时才认）：也是包裹符号 —— **内侧不留空格、外侧按"分隔语言"办**。
 *
 * 内侧：`" + "` → `"+"`、`" 引文 "` → `"引文"`（英文符号 2 的"突出里面内容"）。
 * 外侧：西文内容旁留一格（`He said "hello" loudly`），中文旁贴紧
 * （`他说 "你好" 了` → `他说"你好"了`，即"左引号前、右引号后没有空格"）。
 * 落单的引号（`2" 的管子`）不成对，一个字符都不动。
 */
export const OPEN_QUOTE_RULE: SymbolRule = {
	left: 'space',
	right: 'none',
	source: '英文符号 2（包裹符号）',
	scope: 'latin',
};
export const CLOSE_QUOTE_RULE: SymbolRule = {
	left: 'none',
	right: 'space',
	source: '英文符号 2（包裹符号）',
	scope: 'latin',
};

/**
 * 包裹符号（`||`）：前后都不加空格（latex 符号格式 2）。外侧给 `keep` ——
 * 紧贴内容的那一侧本来就贴着，而"范数 ||x|| 的写法"这种外侧留白是文字间距规则的事，
 * 包裹符号不该去删它。
 */
export const PACKAGE_RULE: SymbolRule = { left: 'keep', right: 'keep', source: 'latex 符号格式 2' };

/** 紧贴这些字符的竖线属于数学记号（`|x|`、`P(A|B)`、`x̂_{k|k}`、`\left\|`），不按正文符号处理 */
const MATH_GLUE_RE = /[A-Za-z0-9_^{}\\]/;

/** 组合用记号（`x̂` 里的 `̂` U+0302）：它也算"贴着字母"，竖线紧挨着它同样属于数学记号 */
const COMBINING_FROM = '\u0300';
const COMBINING_TO = '\u036f';

/** 这个字符算不算"数学记号的一部分" */
export function isMathGlue(char: string): boolean {
	return MATH_GLUE_RE.test(char) || (char >= COMBINING_FROM && char <= COMBINING_TO);
}

/**
 * 广义符号字符集合：只为判断"这个标点是不是在被罗列、被解释"用，不参与空格表。
 *
 * 笔记里大量出现 `1. , /. /! /? /:：后面有空格`、`|：单独一个 | 左右要加空格`、
 * `如, ：` 这类**在讲符号本身**的写法：夹在两个符号中间的标点是"被提到的符号"，
 * 不该被当成中文标点换成全角，也不该被别的规则顺手删掉空格。
 */
export const SYMBOL_CHARS = new Set<string>([...'/\\=+<>*~^_#@%$&|→×÷±≤≥≠']);

/** 这个字符算不算"广义符号" */
export function isSymbolChar(char: string): boolean {
	return SYMBOL_CHARS.has(char);
}
