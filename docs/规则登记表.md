# 规则登记表

> 本文件由 `src/rule-registry.ts` 生成，**请勿手改**。
>
> 重新生成：`node test/run-tests.mjs --update-rules-doc`（`npm test` 会核对它与表是否一致）。
>
> 规范出处指 vault 里的 `data/data note/note note.md`；改规范或改实现时，先改 `src/rule-registry.ts`，再重新生成这份文档。

状态：`done` 有实现且与规范一致 · `partial` 实现了但有已知偏差 · `spec-only` 规范里有、代码里没有。

## 图片功能

| ID | 规则 | 规范出处 | 状态 | 开关 | 实现 | 测试 |
| --- | --- | --- | --- | --- | --- | --- |
| `image.transfer` | 外部绝对路径图片导入仓库，链接换成内部双链 | — | done | `attachmentLocation` `customAttachmentFolder` | `src/image/transfer.ts`<br>`transferExternalImages` | — |
| `image.attachment-folder` | 附件文件夹定位与按需创建 | — | done | `attachmentLocation` `customAttachmentFolder` | `src/image/attachment-folder.ts`<br>`resolveAttachmentFolder` `getTargetAttachmentFolder` `ensureFolder` `parentPathOf` | — |
| `image.garbled` | 乱码图片改名（特殊字符 / URL 编码残留 / 纯数字点号） | — | done | — | `src/image/rename.ts`<br>`isGarbledImageName` `renameGarbledImages` | — |
| `image.rename-preset` | 按预设改名（含强制模式与「已符合格式就保留原名」） | — | done | `imageNamePreset` | `src/image/rename.ts`<br>`renameImagesToPreset` `countImages` | — |
| `image.unique-name` | 文件名在仓库里唯一（四层检查，重名往后推一秒） | — | done | — | `src/image/naming.ts`<br>`generateUniqueTargetPath` `buildVaultBasenameMap` | — |
| `image.same-name` | 同名图片歧义时不猜（宁可跳过，也不改错文件） | — | done | — | `src/image/links.ts`<br>`resolveImageLink` `buildBasenameIndex` `linkBasename` | `test/image-organizer.test.ts` |
| `image.link-format` | 链接写法：完整路径还是仅文件名（同名时强制完整路径） | — | done | `renameLinkFormat` | `src/image/links.ts`<br>`chooseLinkTarget` `isImagePath` `buildCopyName` | `test/image-organizer.test.ts` |
| `image.link-normalize` | 改名后统一修正全库链接格式（保留 `#片段`） | — | done | `renameLinkFormat` | `src/image/rename.ts`<br>`fixImageLinkFormats` | — |
| `image.organize` | 整理图片位置：把别处的图片复制进本笔记的附件夹 | — | done | `attachmentLocation` `customAttachmentFolder` `renameLinkFormat` | `src/image/organize.ts`<br>`organizeNoteImages` | `test/image-organizer.test.ts` |
| `image.size` | 图片大小批改（宽 / 高 / 是否覆盖已有尺寸） | — | done | `imageSizeWidth` `imageSizeHeight` `imageSizeOverwrite` | `src/image/size.ts`<br>`applyImageSize` `validateImageSize` `toSizeString` | `test/image-size.test.ts` |
| `image.external-path` | 弹性路径解析：URL 编码、Markdown 转义、大小写不一致都能找到文件 | — | done | — | `src/image/external-path.ts`<br>`flexibleProbing` `resolvePhysicalPath` | — |
| `image.extension-sets` | 两套扩展名表：受管位图（会改名）与图片链接判定（更宽） | — | done | — | `src/image/constants.ts`<br>`MANAGED_IMAGE_EXTENSIONS` `MANAGED_IMAGE_EXT_RE` `wikiEmbedRe` `externalImageRe` | — |

备注：

- `image.transfer`：依赖 Obsidian 的 vault / fileManager API，没有单测；附件夹定位另有 image.attachment-folder 一条
- `image.attachment-folder`：「只算路径」与「真的要写才创建」分成两步，避免给没有图片的笔记凭空建空文件夹
- `image.garbled`：三条判据见 isGarbledImageName 的注释；改名走 Obsidian 原生 renameFile，所以没有纯函数单测
- `image.rename-preset`：预设 → 正则的转换在 image/naming.ts 的 matchesNamePreset
- `image.unique-name`：裸文件名链接一旦撞名，笔记可能显示成另一张图，所以这条是图片功能的硬约束
- `image.external-path`：直接读磁盘，测试需要真实文件系统，故只有手动验证
- `image.extension-sets`：刻意不合并：合并会让改名功能开始碰 .svg，或让 avif 链接解析不出来（见文件头注释）

## 排版格式 · 标点与符号

| ID | 规则 | 规范出处 | 状态 | 开关 | 实现 | 测试 |
| --- | --- | --- | --- | --- | --- | --- |
| `punct.half-width` | 半角标点 `, . ! ? :` 前不留空格、后空一格 | 格式 / 标点符号 / 英文符号 · 第 1 条 | done | `spacingHalfPunct` | `src/text/symbols.ts`<br>`SYMBOL_RULES` | `test/spacing.test.ts` |
| `punct.quote` | 包裹符号：引号自己不添空格（内侧贴紧，外侧按「分隔语言」办） | 格式 / 标点符号 / 通用符号 · 第 3 条 | done | `spacingSymbolPad` | `src/text/symbols.ts`<br>`OPEN_QUOTE_RULE` `CLOSE_QUOTE_RULE` | `test/spacing.test.ts` |
| `punct.ellipsis` | `...` 前后不留空格 | 格式 / 标点符号 / 英文符号 · 第 3 条 | done | `spacingSymbolPad` | `src/text/spacing/tokenize.ts`<br>`markEllipsisRuns` | `test/spacing.test.ts` |
| `punct.en-modifier` | 英文里修饰符号与所修饰的词看作整体（`he is the "man"` 周围保留词距） | 格式 / 标点符号 / 英文符号 · 第 2 条 | done | `spacingSymbolPad` | `src/text/symbols.ts`<br>`OPEN_QUOTE_RULE` `CLOSE_QUOTE_RULE` | `test/spacing.test.ts` |
| `punct.bracket` | 包裹符号：括号自己不添空格（内侧贴紧，外侧按「分隔语言」办） | 格式 / 标点符号 / 通用符号 · 第 3 条 | done | `spacingBracketInner` | `src/text/spacing/gap.ts`<br>`decideGap` | `test/spacing.test.ts` |
| `punct.package` | 包裹符号 `||` 前后不加空格（`范数 ||x||` 的外侧留白不删） | 格式 / 标点符号 / 通用符号 · 第 3 条 | done | `spacingSymbolPad` | `src/text/symbols.ts`<br>`PACKAGE_RULE` | `test/spacing.test.ts` |
| `punct.full-width` | 全角标点两侧不留空格（引号与书名号内部除外） | 格式 / 文字格式 / 概论 · 第 1 条 | done | `spacingFullPunct` | `src/text/spacing/tokenize.ts`<br>`classifyChar` | `test/spacing.test.ts` |
| `punct.language-switch` | 标点全半角按语境切换（中文句用全角、英文句用半角） | 格式 / 标点符号 / 概论 · 第 1 条 | done | `spacingHalfToFullPunct` | `src/text/spacing/tokenize.ts`<br>`convertHalfPunct` `convertFullPunct` `sentenceLanguages` | `test/spacing.test.ts` |
| `punct.symbol-adjacent` | 符号与符号之间没有空格（`,：`、`(+)`、`"+"`） | 格式 / 标点符号 / 概论 · 第 4 条 | done | `spacingSymbolPad` | `src/text/spacing/gap.ts`<br>`decideGap` | `test/spacing.test.ts` |
| `symbol.cjk-adjacent` | 中文与符号之间不加空格（`瑞克&莫蒂`） | 格式 / 标点符号 / 中文符号 · 第 1 条 | done | `spacingSymbolPad` | `src/text/symbols.ts`<br>`SYMBOL_RULES` | `test/spacing.test.ts` |
| `symbol.ampersand` | `&` 与前后内容之间留一格（正文里只与西文内容留） | 格式 / 标点符号 / 通用符号 · 第 2 条 | done | `spacingSymbolPad` | `src/text/symbols.ts`<br>`SYMBOL_RULES` | `test/spacing.test.ts` |
| `symbol.dollar-inside` | `$` 内侧永远不空（Obsidian 能否认出公式的前提） | 格式 / 标点符号 / 通用符号 · 第 2 条 | done | `spacingMathText` | `src/text/inline-scan.ts`<br>`readInlineMath` | `test/spacing.test.ts`<br>`test/latex-layout.test.ts` |
| `symbol.modifier` | 修饰符号不加空格（`^`、单独一个 `|`） | 格式 / 标点符号 / 通用符号 · 第 3 条 | done | `spacingSymbolPad` | `src/text/symbols.ts`<br>`SYMBOL_RULES` `STANDALONE_PIPE_RULE` | `test/spacing.test.ts` |
| `symbol.relation` | 运算 / 关系符号与前后内容之间加空格（`→`） | 格式 / 标点符号 / 通用符号 · 第 4 条 | done | `spacingSymbolPad` | `src/text/latex.ts`<br>`RELATION_COMMANDS` `RELATION_CHARS` `separator` | `test/spacing.test.ts`<br>`test/latex-layout.test.ts` |
| `symbol.spacing-command` | 空格符号看作空格，与前后相连（`x\, dr`） | 格式 / 标点符号 / 通用符号 · 第 5 条 | done | `mathLayout` | `src/text/latex.ts`<br>`SPACING_COMMANDS` `SINGLE_SPACING_COMMANDS` `isSpacingOnly` `splitGluedSpacing` | `test/latex-layout.test.ts` |
| `symbol.no-rule` | 符号表里没有的符号一律不动（`~`、`-` 连字符） | 格式 / 标点符号 / 通用符号 · 第 6 条 | done | — | `src/text/spacing/gap.ts`<br>`decideGap` | `test/spacing.test.ts` |

备注：

- `punct.quote`：与 punct.bracket 同一条语义：**包裹符号自己不添空格** —— 内侧贴紧（`" 引文 "` → `"引文"`、`" + "` → `"+"`），外侧不主动加也不删语言自带的词距（中文旁贴紧 `他说 "你好" 了` → `他说"你好"了`；西文旁留一格 `He said "hello" loudly`，那是英文词距）。落单的引号（`2" 的管子`）不成对，一个字符都不动
- `punct.en-modifier`：实现机制是 SymbolRule.scope = latin：修饰符号的 `space` 只对**西文内容**生效 —— 所以英文旁按词距留一格（`he is the "man"` 原样；漏了就补，`the"man"` → `the "man"`），中文旁贴紧（`他说 "你好" 了` → `他说"你好"了`）。中文侧写的是「内外均没有空格」（通用符号 3 的包裹符号子条目），英文侧写的是这条 —— 两句话说的是同一件事的两面
- `punct.bracket`：「内外均没有空格」的意思是**包裹符号自己不添空格**，不是"把外侧已有的空格也删掉"：内侧的填充空格删掉（`( x )` → `(x)`），外侧不主动加、也不删**语言自带**的词距 —— 西文旁那一格是英文词距（`appendix (page 3)`），中文旁本来就没有（`中文 (说明)` → `中文(说明)`、`he said 你好(nihao)`）。`||` 这种包裹符号另见 punct.package
- `punct.package`：连续两个及以上的 `|` 整体当包裹符号，按 通用符号 3 的包裹符号子条目贴紧；外侧给 `keep`：紧贴内容那一侧本来就贴着，而"范数 ||x|| 的写法"这种外侧留白属于文字间距规则，包裹符号不该去删它
- `symbol.cjk-adjacent`：实现方式是 SymbolRule.scope = latin：`space` 只对西文内容生效，中文旁与符号旁一律贴紧
- `symbol.dollar-inside`：识别规则只有一份（inline-scan 的 readInlineMath），公式排版 latex.ts 判断行内公式收尾也用它 —— 2026-09 收敛后不再各写一份
- `symbol.modifier`：包裹符号（`()`、`||`、引号）归在「通用符号 3」的包裹符号子条目下，见 punct.bracket、punct.quote 与 punct.package
- `symbol.no-rule`：规范第 6、7 条要求 `~`、`-` 两边不加空格，实现方式是**故意不进符号表**（见 symbols.ts 表后的说明）：既不添空格，也不删作者写的空格 —— `甲 ~ 乙` 与 `甲~乙` 都原样保留（`约 ~5 个` 里少掉的那一格来自"中文 ↔ 数字"规则，与 `~` 无关）。排版不替作者补他没写的东西，也不删他写的东西

## 排版格式 · 文字与数字

| ID | 规则 | 规范出处 | 状态 | 开关 | 实现 | 测试 |
| --- | --- | --- | --- | --- | --- | --- |
| `text.cjk-latin` | 中文 ↔ 英文之间空一个字宽 | 格式 / 文字格式 / 概论 · 第 1 条 | done | `spacingCjkLatin` | `src/text/spacing/gap.ts`<br>`decideGap` | `test/spacing.test.ts` |
| `text.cjk-digit` | 中文 ↔ 数字之间不留空格（`第 3 章` → `第3章`） | 格式 / 数字格式 · 第 2 条 | done | `spacingCjkDigit` | `src/text/spacing/gap.ts`<br>`decideGap` | `test/spacing.test.ts` |
| `text.latin-digit` | 英文 ↔ 数字之间：默认保持原样（专有名词优先） | 格式 / 数字格式 · 第 3 条 | done | `spacingLatinDigit` | `src/text/spacing/gap.ts`<br>`decideGap` | `test/spacing.test.ts` |
| `text.math-gap` | 行内公式 ↔ 文字之间空一格（只动 `$` 外面） | 格式 / 文字格式 / 概论 · 第 1 条 | done | `spacingMathText` | `src/text/spacing/gap.ts`<br>`decideGap` | `test/spacing.test.ts` |
| `text.digit-unit` | 数字 ↔ 单位之间空一格（单位须在词表里） | 格式 / 标点符号 / 数学符号 · 第 2 条 | done | `spacingDigitUnit` | `src/text/spacing/gap.ts`<br>`UNITS` `decideGap` | `test/spacing.test.ts` |
| `text.proper-noun` | 专有名词的空格以原有形式为准（《新 吊带袜天使》） | 格式 / 文字格式 / 概论 · 第 4 条 | done | — | `src/text/spacing/tokenize.ts`<br>`markTitlePieces` | `test/spacing.test.ts`<br>`test/text-math.test.ts` |
| `text.english-word` | 带其他符号的英文词按单词整体处理（`GPT4`、`v1.2.2`） | 格式 / 文字格式 / 英文 · 第 1 条 | done | `spacingCjkLatin` | `src/text/spacing/tokenize.ts`<br>`markAlphanumericWords` `markAbbreviationPieces` | `test/spacing.test.ts` |
| `text.use-space-sparingly` | 不要滥用空格：空格只用来分隔不同语言的内容 | 格式 / 标点符号 / 概论 · 第 3 条 | done | `spacingSymbolPad` | `src/text/spacing/gap.ts`<br>`decideGap` | `test/spacing.test.ts` |

备注：

- `text.cjk-latin`：行内代码、双链、链接、标签与英文等价（标记命名 10）
- `text.proper-noun`：中文与中文之间的空格不动；书名号与引号内部的字符一个都不动（math-wrap.ts 的 protectTitles 同理）

## 排版格式 · 结构与顺序

| ID | 规则 | 规范出处 | 状态 | 开关 | 实现 | 测试 |
| --- | --- | --- | --- | --- | --- | --- |
| `structure.tag-placement` | 标签归位：写到内容最后，与正文空一格 | 格式 / 标签格式 · 第 1 条 | done | `tagLayout` | `src/text/tags.ts`<br>`formatTags` `layoutBlock` `stripTags` | `test/tags.test.ts` |
| `structure.tag-sort` | 同一处的多个标签按首字母排序 | 格式 / 概论 · 第 4 条 | done | `tagSort` | `src/text/tags.ts`<br>`orderTags` | `test/tags.test.ts` |
| `structure.block-sort` | 内容板块按首字母排序（只在同类相邻块之间） | 格式 / 概论 · 第 4 条 | done | `blockSort` | `src/text/block-sort.ts`<br>`sortContentBlocks` | `test/block-sort.test.ts` |
| `structure.collate` | 首字母比较：中文按拼音、数字按数值、大小写与全半角不敏感 | 格式 / 概论 · 第 4 条 | done | — | `src/text/collate.ts`<br>`compareByFirstLetter` | `test/tags.test.ts`<br>`test/block-sort.test.ts` |
| `structure.indent` | 行首缩进归一：4 个空格算一个 tab，零散空格删掉 | 格式 / 标点符号 / 通用符号 · 第 1 条 | done | `textLeadingIndentFix` | `src/text/indent.ts`<br>`fixLeadingIndent` `resolveLeadingIndentMode` | `test/text-layout.test.ts` |
| `structure.markers` | 块级标记空白：引用 `>` 后补空格、列表与标题符号后的多个空格收成一个 | 命名 / 标记命名 · 第 15 条 | done | `textLeadingIndentFix` | `src/text/markers.ts`<br>`fixBlockMarkers` | `test/markdown-markers.test.ts` |
| `structure.list-number` | 列表序号整理：保证每个列表的首项编号是 1 | — | done | `listRenumber` | `src/text/list-numbering.ts`<br>`fixListNumbers` | `test/list-numbering.test.ts` |
| `structure.heading-level` | 标题级别整理：子标题与父标题恰好差一级 | — | done | `headingLevelFix` | `src/text/heading-levels.ts`<br>`fixHeadingLevels` | `test/heading-levels.test.ts` |
| `structure.chat-log` | 聊天记录排版：头部信息与正文缩进、图文顺序、消息间空行 | — | done | `chatShowUsername` `chatShowDate` `chatShowTime` `chatIndent` `chatImageOrder` `chatBlankLineBetweenMessages` | `src/text/chat-log.ts`<br>`formatChatLog` `resolveIndent` | `test/chat-log.test.ts` |

备注：

- `structure.indent`：规范「通用符号 1」只定了 tab 的宽度（一个 tab 4 个字宽），"哪些空格删、列表子项保留、标记空白"是实现约定（另见 structure.markers）。`off` 同时是整块文本排版的总开关
- `structure.list-number`：规范里没有"序号从 1 开始"这一条；相关的是 数字格式 第 1 条（需要用几位数字就占几位）—— 实现照此不加前导零。首项已经是 1 的列表一律不动（`1. 1. 1.`、`1. 5. 9.` 都是作者故意写的形态，block-sort.ts 的编号整理也保留这两种）
- `structure.heading-level`：规范里没有"父子标题差一级"这一条；相关的是 模板 / 内容模板应用 / 标题分级 第 1 条（引用其他笔记的标题内容时要额外写一个相同的标题防止分级错误）—— 所以首个标题的级别保持原样，只压缩父子之间的跳级。新级别全部从原始级别一次算出，多个标题同时改时不会互相串
- `structure.chat-log`：规范里没有对应条目；必须严格幂等（重复执行不能再产生空行或空格变化）

## 代码格式 · 公式

| ID | 规则 | 规范出处 | 状态 | 开关 | 实现 | 测试 |
| --- | --- | --- | --- | --- | --- | --- |
| `math.latex-only` | 公式和符号都用 latex 语法打（正文数学符号自动包 `$…$`） | 格式 / 代码格式 · 第 4 条 | done | `textMathWrapSymbols` | `src/text/math-wrap.ts`<br>`wrapPlainMath` `mathStretches` `isMathLike` `isMathContext` `renderStretch` | `test/text-math.test.ts` |
| `math.space-equals-render` | 代码里的空格 = 公式渲染出来的空格（只在 `\\` 处换行） | 格式 / 代码格式 · 第 3 条 | done | `mathLayout` | `src/text/latex.ts`<br>`tokenize` `separator` `renderTokens` | `test/latex-layout.test.ts` |
| `math.brace` | 拼接会吃掉命令名时补花括号（`\delta x` → `\delta{x}`） | 格式 / 代码格式 · 第 3 条 | done | `mathLayout` | `src/text/latex.ts`<br>`needsBrace` | `test/latex-layout.test.ts` |
| `math.indent` | 公式代码不额外缩进；续行缩进 = 首行缩进 + 1 个 tab | 格式 / 代码格式 / 公式代码格式 · 第 1 条 | done | `mathLayout` | `src/text/latex.ts`<br>`leadingWhitespace` `formatDisplayBlocks` | `test/latex-layout.test.ts` |
| `math.big-operator` | 大型运算符（`\begin{}` / `\end{}`）连环境名当一个整体，前后不加空格 | 格式 / 代码格式 / 公式代码格式 · 第 2 条 | done | `mathLayout` | `src/text/latex.ts`<br>`tokenize` `separator` | `test/latex-layout.test.ts` |
| `math.unary` | 正负号等修饰符号前后没有空格（一元 / 二元按上下文判断） | 格式 / 标点符号 / 数学符号 · 第 3 条 | done | `mathLayout` | `src/text/latex.ts`<br>`isUnary` `SIGN_CHARS` | `test/latex-layout.test.ts` |
| `math.relation-gap` | 公式里运算符号 / 关系符号前后加空格，列举逗号后加空格 | 格式 / 标点符号 / 数学符号 · 第 1 条 | done | `mathLayout` | `src/text/latex.ts`<br>`separator` `RELATION_COMMANDS` | `test/latex-layout.test.ts` |
| `math.math-mode-text` | 公式里裸写的中文包进 `\text{…}` | 格式 / 代码格式 · 第 4 条 | done | `mathLayout` | `src/text/latex.ts`<br>`wrapMathText` `FULL_WIDTH_RE` | `test/latex-layout.test.ts` |
| `math.derivative-space` | 微分算子前加薄空格（`f(x)\, dr`） | 格式 / 标点符号 / 数学符号 · 第 5 条 | spec-only | — | `src/text/latex.ts`<br>`SINGLE_SPACING_COMMANDS` | `test/latex-layout.test.ts` |

备注：

- `math.latex-only`：规范只写了"用 latex 语法打"；判定规则（数学语境词、量词、变量表、专有名词与缩写排除）是实现补充，见模块头注释
- `math.derivative-space`：代码只保留作者自己写好的 `\,`（当普通间距命令处理），**不自动补**：微分算子是写法问题，排版不该替作者补他没写的内容（2026-09 明确决定，别再"补全"）

## 横切机制

| ID | 规则 | 规范出处 | 状态 | 开关 | 实现 | 测试 |
| --- | --- | --- | --- | --- | --- | --- |
| `cross.protect-lines` | 整行保护区：frontmatter、围栏 / 缩进代码块、GFM 表格 | — | done | — | `src/text/line-scan.ts`<br>`markProtectedLines` `markIndentedCodeLines` `markTableLines` `inlineCodeRanges` | `test/spacing.test.ts`<br>`test/tags.test.ts`<br>`test/block-sort.test.ts` |
| `cross.protect-inline` | 行内保护区：行内代码、双链与链接、URL、HTML、注释、标签 | — | done | — | `src/text/inline-scan.ts`<br>`collectMaskedRanges` `isSpaceChar` | `test/spacing.test.ts`<br>`test/text-math.test.ts` |
| `cross.math-recognition` | `$$…$$` 区块与行内 `$…$` 的识别与配对 | — | done | — | `src/text/inline-scan.ts`<br>`readInlineMath` `dollarPositions` `dollarMarks` `mathRanges` `mathOpaqueLines` | `test/spacing.test.ts`<br>`test/text-math.test.ts`<br>`test/tags.test.ts` |
| `cross.pipeline` | 流水线顺序与不动点迭代：每步幂等，只在真正变化时写盘 | — | done | — | `src/text/pipeline.ts`<br>`formatNoteText` | `test/text-pipeline.test.ts` |
| `cross.batch` | 批量外壳：互斥锁 + 通知屏蔽 + 状态栏进度，单篇失败不拖垮整批 | — | done | — | `src/batch.ts`<br>`BatchRunner` | `test/commands.test.ts` |

备注：

- `cross.protect-lines`：规范里没写，但改坏代码块是排版类功能最大的风险
- `cross.math-recognition`：识别与配对**只有这一份实现**（2026-09 收敛）：行内 `$…$` 用 readInlineMath、`$$` 位置用 dollarPositions、按行配对用 mathRanges，行级消费者（标签排版、板块排序）问"这行能不能碰"就用 mathOpaqueLines。以前散着 6 份（还有 line-scan.markMathLines 与 latex.ts 的 collectMarks / findInlineMathEnd），其中两套配对算法对"一行内成对 `$$…$$`"给出不同答案 —— 标签排版当整行保护区、空格排版当整行可排版，于是含公式行上的标签不归位。现在两种问法同源：跨行区块的行与起止行整行跳过，一行内成对的行照常排版
- `cross.pipeline`：顺序理由写在 pipeline.ts 头部；整条链迭代到不动点（最多 5 轮），因为后面的步骤会改前面的步骤看过的文本
